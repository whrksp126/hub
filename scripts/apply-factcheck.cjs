/**
 * 8개 프로젝트 담당 에이전트의 점검 보고서(RESUME-PORTFOLIO-REVIEW.md) 결과를
 * 포트폴리오 projects 테이블에 반영한다.
 *
 * 원칙
 *  - 재현 근거가 없는 수치는 뺀다(정성 표현으로 대체).
 *  - 코드와 다른 서술은 코드에 맞춘다.
 *  - 지금 UI에서 쓸 수 없는 기능은 빼거나 실제 동작으로 바꾼다.
 *  - 문체는 기존 톤(인사담당자용 평이체)을 유지한다.
 *
 * 문자열 치환이므로 멱등하다(이미 반영됐으면 건너뛴다).
 * 실행: node scripts/apply-factcheck.cjs   /   docker exec hub_app_prod node /app/apply-factcheck.cjs
 */
const Database = require('better-sqlite3')
const db = new Database(process.env.DATABASE_PATH || './data/hub.db')
const now = Math.floor(Date.now() / 1000)

// slug -> { fields:{}, replace:[[old,new],…], dropMedia:[filename,…] }
const PLAN = {
  // ────────────────────────────────────────────────────────── GHC
  ghc: {
    fields: { url: 'ghc.ghmate.com' },
    replace: [
      // 도달 불가한 '화면·창 캡처' 대신 실제로 되는 '전용 창' 서술로
      ['OBS 같은 방송 프로그램 없이 앱이 화면과 소리를 직접 잡아 내보내고',
       'OBS 같은 방송 프로그램 없이 앱이 전용 창을 직접 잡아 내보내고'],
      ['방송 프로그램이 필요 없다 — 앱이 화면과 소리를 직접 잡아 내보낸다',
       '방송 프로그램이 필요 없다 — 앱이 전용 창을 직접 잡아 내보낸다'],
      ['화면·창 방송 — OBS 없이 앱이 직접 캡처한다. 특정 앱의 소리만 골라 넣을 수도 있다',
       '고화질 라이브 — 원본 그대로 약 12초 버퍼로 재생하고, 회선이 약한 시청자에겐 720p를 따로 만들어 준다'],
      // 대역폭: 가정 기반 계산값을 실측으로 표기하던 것 정정
      ['{"value":"140~250 vs 87 Mbps","label":"동시 45개 방에 필요한 대역폭 vs 집 서버의 실제 회선 (실측)"}',
       '{"value":"100Mb 회선 · 동시 8개","label":"집 서버 회선을 실측해 동시 방송 상한을 코드로 고정"}'],
      ['{"value":"9일 무중단","label":"집 서버 컨테이너 7개 · 유휴 시 CPU 1.58% · 메모리 336MB"}',
       '{"value":"유휴 CPU 1.5%","label":"집 서버 한 대, 컨테이너 8개 (2026-09 측정)"}'],
      ['그래서 만들기 전에 계산부터 했습니다. 동시에 45개 방을 쓰면 140~250Mbps가 필요한데 실제 회선은 87Mbps였습니다. 그 숫자에 맞춰 화질 상한과 동시 방송 개수를 정했고, 지금 컨테이너 7개가 9일째 재시작 없이 돌고 있습니다.',
       '그래서 만들기 전에 회선부터 쟀습니다. 랜카드가 100Mb짜리고 실제 업로드는 87Mbps였습니다. 그 숫자에 맞춰 동시 방송 개수를 정했고, 지금 컨테이너 8개가 배포 사이에 재시작 없이 돌고 있습니다.'],
      ['만들기 전에 한계를 먼저 쟀다 — 필요한 대역폭 140~250Mbps에 실제 회선은 87Mbps. 송출 화질을 3.5에서 2.0Mbps로 낮추고 동시 방송 8개, 사용자당 카메라 3대로 상한을 걸어 회선이 막히는 상황을 사전에 없앴다',
       '만들기 전에 한계를 먼저 쟀다 — 랜카드 100Mb에 업로드 실측 87Mbps. 동시 방송 8개, 사용자당 카메라 3대로 상한을 코드에 걸어 회선이 막히는 상황을 사전에 없앴다'],
      // 1.99초·220ms: HLS 버퍼 도입(08-17) 이후 성립하지 않음
      ['재생을 OS 기본 엔진에 맡기는 전용 창을 만들어 우회했고, 창이 뜨는 데 220ms, 방송 버튼을 누르면 시청자 화면에 1.99초 만에 도착한다',
       '재생을 OS 기본 엔진에 맡기는 전용 창을 만들어 우회했고, 방송 버튼을 누르기 전에는 아무것도 나가지 않도록 창 열기와 송출을 분리했다'],
      // viewer 역할은 ENUM에만 존재
      ['방 관리 — 8자리 코드와 QR로 초대, 선택 비밀번호, 방장·멤버·뷰어 권한 구분',
       '방 관리 — 8자리 코드·QR 초대, 선택 PIN, 방장/참여자 구분과 시청 전용 입장'],
      // 배포 버전
      ['(현재 macOS 0.1.4)', '(현재 macOS 0.1.33 · Windows 0.1.4)'],
      ['Windows 배포가 0.1.1에 멈춰 있다. 릴리스 과정을 양쪽으로 맞추는 일이 남았다',
       'Windows는 수동 빌드라 macOS보다 릴리스가 뒤처진다(0.1.4). 릴리스 과정을 양쪽으로 맞추는 일이 남았다'],
      ['이 페이지의 수치는 전부 실제로 측정한 값이다. 반대로 서버 교체 전후 정량 비교와 실사용 부하는 측정하지 못했고, 그대로 남겨 둔다',
       '회선·자원 수치는 실측이고, 필요 대역폭은 가정에 기반한 추정이다. 서버 교체 전후 정량 비교와 실사용 부하는 측정하지 못했다'],
      // 구성도 캡션: 이미지가 아직 7개 기준이라 시점을 밝힌다
      ['클라이언트는 웹 하나, 서버는 집 컴퓨터 한 대 안의 컨테이너 7개',
       '클라이언트는 웹 하나, 서버는 집 컴퓨터 한 대 (그림은 2026-08 기준 7개, 현재 8개)'],
    ],
    dropMedia: ['ghc-room-list.png', 'ghc-room-preflight.png', 'ghc-mobile-room-grid.png'],
  },

  // ────────────────────────────────────────────────── OrderAndGo
  orderandgo: {
    replace: [
      // 연동 단말기는 토스 프론트 단말기 하나뿐
      ['살 게 없다 — 키오스크도 카드리더기도 따로 필요 없다. 손님 폰과 기존 단말기면 된다',
       '키오스크가 필요 없다 — 손님 폰으로 주문하고, 토스 단말기를 쓰는 매장이면 결제까지 바로 연결된다'],
      ['새 결제기를 사거나 별도 계약을 할 필요 없이, 매장에서 쓰던 단말기를 그대로 씁니다.',
       '토스 단말기를 쓰는 매장이라면 새 결제기 없이 그대로 연결해 씁니다.'],
      // 카드 색이 아니라 상태 배지가 바뀐다 / 버튼 이름은 '완료처리'
      ['주방에서 조리 완료를 누르면 POS의 테이블 색이 바뀌고, 직원 호출도 같은 방식으로 전달됩니다.',
       '주방에서 완료를 누르면 POS의 테이블 상태가 ‘조리완료’로 바뀌고, 직원 호출도 같은 방식으로 전달됩니다.'],
      ['주방 화면 — 만들 주문만 크게, 조리 완료를 누르면 POS 테이블 색이 바뀐다',
       '주방 화면 — 만들 주문만 크게, 완료를 누르면 POS 테이블 상태가 바뀐다'],
      // 주방 티켓 출력은 미구현(console.log만)
      ['결제·영수증 — 매장 단말기로 카드·현금·현금영수증, 결제 취소와 나눠 내기, 주방 티켓 자동 출력',
       '결제·영수증 — 매장 단말기로 카드·현금·현금영수증, 결제 취소와 나눠 내기, 종이 영수증 출력'],
      // 238ms 측정 근거 없음
      ['{"value":"238ms → 즉시","label":"POS 화면 전환 (캐시 우선 렌더)"}',
       '{"value":"200m","label":"매장 반경 밖 QR 주문 차단 (위치 확인)"}'],
      ['POS 화면을 누를 때마다 서버 응답 238ms를 기다리고 전환 효과 150ms가 더 붙었다 — 저장해 둔 화면을 먼저 그리도록 바꿔 누르는 즉시 뜨게 했다',
       'POS 화면을 누를 때마다 서버 응답과 전환 효과를 기다렸다 — 한 번 불러온 화면을 먼저 그리고 뒤에서 갱신하도록 바꿔 다시 들어갈 때 바로 뜨게 했다'],
      // 드래그 겹침 되돌림은 현재 동작하지 않음(2026-04-03 회귀)
      ['자리 배치가 저장 버튼을 눌러야 반영됐다 — 끌어다 놓고 0.5초 뒤 자동 저장되게 바꾸고, 겹치면 되돌리는 판정을 외부 라이브러리 없이 직접 만들었다',
       '자리 배치가 저장 버튼을 눌러야 반영됐다 — 끌어다 놓고 0.5초 뒤 자동 저장되게 바꾸고, 격자 맞춤·크기 조절 판정을 외부 라이브러리 없이 직접 만들었다'],
      // 실사용 매장은 코드로 확인 불가
      ['실제로 돌아간다 — 데모 매장까지 갖춰 지금도 운영 중',
       '실제로 돌아간다 — 데모 매장까지 갖춰 공개 운영 중'],
      ['직접 정식 런칭하고 지금도 운영 중', '직접 런칭해 공개 운영 중'],
      // 영상 캡션: POS가 나오지 않음
      ['테이블에서 주문하면 주방 화면에 새 주문이 뜨고, 조리 완료를 누르면 POS에 반영된다',
       '테이블에서 주문하면 주방 화면에 바로 뜨고, 완료처리하면 목록에서 빠진다'],
    ],
    dropMedia: ['staff-call-dual.mp4'],
  },

  // ───────────────────────────────────────────────────── Lambent
  lambent: {
    replace: [
      // 0.25→0.95는 '감지율'이 아니라 비트당 온셋 수
      ['{"value":"0.25 → 0.95배","label":"실제 음원 기준 비트 감지율 (재보정 후)"}',
       '{"value":"4개 중 1개 → 거의 매 비트","label":"조용한 곡에서 잡히는 비트 (로컬 실측)"}'],
      ['{"value":"93.75회/초","label":"오디오 분석 주기 (화면 60프레임과 분리)"}',
       '{"value":"초당 약 94회","label":"오디오 분석 주기, 48kHz 기준 (화면 60프레임과 분리)"}'],
      // 앱은 소리를 내지 않는다 — 되먹임 분리는 착수 전 실험값
      ['{"value":"100~500배","label":"자기 소리 되먹임 분리 (실측)"}',
       '{"value":"권한 1개","label":"화면 기록·마이크 없이 ‘소리 듣기’만"}'],
      ['합성음으로 맞춰둔 감지 기준이 실제 음악에서는 거의 안 먹혔다 — 화면을 보며 조절하는 대신 실제 음원으로 훑어 기준을 다시 잡아, 비트 감지율을 0.25배에서 0.95배까지 회복시키고 회귀 테스트로 고정했다',
       '합성음으로 맞춘 감지 기준이 조용한 곡에서 비트 4개 중 3개를 놓쳤다 — 화면을 보며 조절하는 대신 실제 음원으로 훑어 기준을 다시 잡고, 기준이 흔들리면 실패하는 테스트로 고정했다'],
      ['소리를 다루는 스레드가 멈추면 음이 끊긴다 — 기다리지 않는 3스레드 구조로 분석(초당 93.75회)과 화면(초당 60회)을 분리하고, 분석 단위를 오디오 장치 단위와 1:1로 맞췄다',
       '실시간 소리 스레드가 한 번이라도 기다리면 분석이 밀린다 — 기다리지 않는 3스레드 구조로 분석(초당 약 94회)과 화면(초당 60회)을 분리하고, 분석 단위를 장치가 주는 크기에 맞췄다'],
      ['자기 소리를 다시 듣는 문제 — 가상 드라이버를 설치하지 않고 시스템 소리를 캡처하되, 자기 소리만 100~500배 차이로 분리했다(실측)',
       '가상 드라이버 설치 없이 시스템 소리를 캡처했다 — macOS 기본 기능만 쓰고, 자기 소리 제외는 착수 전 실험에서 100배 가까운 차이로 확인해 두었다'],
      // 앨범 아트 색이라는 게 사진만으로는 드러나지 않음
      ['어두운 배경 — 앨범 아트에서 뽑은 색으로 물든다', '어두운 배경 — 네 변이 빛으로 물든다'],
      // 프리셋 소스 선택은 실제 기능인데 빠져 있었다
      ['메뉴 막대 전용 — Dock 아이콘 없이 메뉴 막대에서만 다룬다. 단축키로 즉시 숨김',
       '소스 선택 — 시스템 전체 또는 특정 앱의 소리만 골라 반응한다. 메뉴 막대에서만 다루고 단축키로 즉시 숨김'],
    ],
    dropMedia: ['lambent-bloom-dark-off.png'],
  },

  // ──────────────────────────────────────────────────── CodingPT
  codingpt: {
    replace: [
      // E2EE 범위: 터미널·화면이 아니라 제어 명령·파일 요청
      ['{"value":"3.4 → 25 fps","label":"실기기·에뮬레이터 화면 전송 (하드웨어 영상 스트림 전환)"}',
       '{"value":"3.4 → 20+ fps","label":"안드로이드 기기 화면 전송 (영상 스트림 전환)"}'],
      ['{"value":"310~420 → 96~109 ms","label":"화면 반응 지연 (3단 경로 설계)"}',
       '{"value":"310~420 → 96~109 ms","label":"같은 Wi-Fi 화면 반응 지연"}'],
      ['화면이 슬라이드쇼처럼 끊겼다 — 스크린샷을 반복해 찍던 방식을 영상 스트림으로 바꿔 3.4fps → 25fps, 대기 중 데이터 사용량은 55KB/s → 6KB/s',
       '화면이 슬라이드쇼처럼 끊겼다 — 스크린샷을 반복해 찍던 방식을 영상 스트림으로 바꿔 초당 3.4장 → 20장 이상, 대기 중 데이터 사용량은 55KB/s → 6KB/s'],
      ['손끝에서 지연이 느껴졌다 — 같은 Wi-Fi면 기기끼리 직접, 밖이면 서버를 우회하는 3단 경로를 만들어 310~420ms → 96~109ms',
       '손끝에서 지연이 느껴졌다 — 같은 Wi-Fi면 기기끼리 직접, 밖이면 WebRTC, 안 되면 서버 중계로 내려가는 3단 경로를 만들어 같은 Wi-Fi 기준 310~420ms → 96~109ms'],
      ['서버도 내용을 못 보게 했다 — 터미널과 화면 데이터를 외부 라이브러리 없이 직접 암호화하고, 구현끼리 호환되는지 테스트로 고정했다',
       '서버도 명령 내용을 못 보게 했다 — 원격 조작 명령과 파일 요청을 외부 라이브러리 없이 직접 암호화하고, 4개 구현이 서로 호환되는지 테스트로 고정했다'],
      // 클라우드 러너는 prod에서 켜져 있고, 후원 구독은 판매 중
      ['계정 정보는 PC 밖으로 나가지 않고, 서버가 대신 부르는 AI 호출은 0건입니다.',
       '내 PC에 연결해 쓰면 계정 정보가 PC 밖으로 나가지 않고, 서버가 대신 부르는 코딩 AI 호출은 0건입니다.'],
      ['PC가 없는 사용자를 위한 클라우드 실행과 구독 결제는 만들어 뒀지만 꺼둔 상태다',
       '개인 사용은 무료이고 월 4,900원 선택 후원만 받는다. 사용량 과금과 앱 안 구독은 만들어 뒀지만 꺼 두었다'],
      // 이어 쓴 것은 코드 실행기가 아니라 모바일 에디터·터미널 화면
      ['교육 앱을 만들며 쌓아둔 코드 실행 기능을 그대로 살려, 폰에서도 AI와 함께 개발을 이어갈 수 있는 도구로 다시 만들었습니다.',
       '교육 앱을 만들며 쌓아둔 모바일 코드 에디터·터미널 화면을 그대로 살려, 폰에서도 AI와 함께 개발을 이어갈 수 있는 도구로 다시 만들었습니다.'],
      ['2026.05 — 제품 저장소를 새로 만들고 서버·웹·관리자를 분리했다. 이후 커밋 저자는 단독',
       '2026.05~06 — 제품 저장소를 새로 만들고 서버·웹·관리자를 분리했다. 이후 커밋 저자는 단독'],
      // 캡션: 미리보기는 PC 쪽 화면에 뜬다
      ['폰에서 명령을 보내면 PC에서 실행되고, 바뀐 결과가 폰 미리보기에 그대로 반영된다',
       '폰에서 명령을 보내면 PC에서 실행되고, 바뀐 결과가 PC 미리보기에 바로 반영된다'],
      ['PC에서 찾은 AI 도구를 골라 연결한다 — 계정 정보는 PC 밖으로 나가지 않는다',
       'PC에 설치된 AI 도구를 찾아 연동 수준과 함께 보여 준다 — 우리가 모델을 고르지 않는다'],
    ],
    dropMedia: ['codingpt-mobile-approval-3.png'],
  },

  // ───────────────────────────────────────────────────── HeyVoca
  heyvoca: {
    replace: [
      // 테스트 113개: 24개는 import 오류로 수집조차 안 됨
      ['{"value":"113개","label":"FSRS-5 자체 구현 전용 테스트"}',
       '{"value":"3단계","label":"기억 상태를 안정도·난이도·회상 확률로 계산"}'],
      ['{"value":"3 → 16","label":"서버 동시 처리 (응답 밀림 해소)"}',
       '{"value":"3 → 16","label":"서버가 한 번에 받는 요청 수"}'],
      ['{"value":"5만여 단어","label":"사용자 DB와 분리 배포되는 사전 DB"}',
       '{"value":"약 5만 단어","label":"사용자 DB와 분리 배포되는 사전 DB"}'],
      ['기존 복습 알고리즘을 버리고 최신 알고리즘을 직접 구현했다 — 서버 환경이 낡아 기존 라이브러리를 쓸 수 없었고, 한 번 틀렸다고 복습 간격이 급락해 학습자가 이탈하는 문제도 막아야 했다. 전용 테스트 113개로 고정하고 무중단 교체',
       '기존 복습 알고리즘을 버리고 최신 알고리즘을 직접 구현했다 — 서버의 구버전 Python에선 기존 라이브러리를 쓸 수 없었고, 한 번 틀렸다고 복습 간격이 급락하지 않게 보정도 넣어야 했다. 단어 상태 컬럼은 그대로 둔 채 교체'],
      // 사전 자동 동기화는 2026-09-04에 제거됨
      ['하나였던 DB를 사용자용과 사전용으로 분리했다 — 5만여 단어 사전을 검증된 배포 파일로 만들어, 서버가 뜨면 자동으로 맞춰지게 했다',
       '하나였던 DB를 사용자용과 사전용으로 분리했다 — 약 5만 단어 사전을 검증된 배포 파일로 만들어, 관리자 화면에서 한 번에 교체·되돌리게 했다'],
      ['응답이 밀리던 문제 — 서버가 동시에 처리하는 양을 3에서 16으로 늘리고 DB 연결 관리를 다시 잡아 해소했다',
       '느린 외부 호출이 서버 전체를 막던 구조 — 한 번에 받는 요청 수를 3에서 16으로 늘리고 DB 연결 관리를 다시 잡아 막히지 않게 했다'],
      // 유료 엔진 운영 기간은 3.5일. 비용 0은 캐싱이 아니라 무료 엔진 전환
      ['유료였던 발음 기능을 무료 엔진으로 무중단 전환했다 — 엔진을 갈아끼울 수 있는 구조로 만들고 결과를 캐싱해 비용을 없앴다',
       '발음 엔진을 갈아끼울 수 있게 만들고, 한 번 만든 음성은 저장해 재사용했다 — 유료 엔진을 무료 엔진으로 설정 한 줄에 교체'],
      ['복습 알고리즘 교체, 발음 비용 절감, DB 분리', '복습 알고리즘 교체, 발음 엔진 교체·캐싱, DB 분리'],
      // 사람별 파라미터는 미구현 — 단어별 기록이 다른 것
      ['잊기 전에 다시 — 답한 속도와 단어 난이도까지 반영해 사람마다 다른 복습일을 계산한다',
       '잊기 전에 다시 — 답한 속도와 단어 난이도까지 반영해 단어마다 복습일을 따로 계산한다'],
      ['복습 알고리즘 — 자주 틀린 단어를 먼저, 사람마다 다른 복습일을 계산한다',
       '복습 알고리즘 — 자주 틀린 단어를 먼저, 단어마다 복습일을 따로 계산한다'],
      // 문제 유형은 6가지
      ['5종 테스트 — 사지선다·카드매치·듣기·빈칸에 콤보와 오답 재출제까지',
       '6가지 문제 — 사지선다(정·역방향)·빈칸·카드 맞추기에 듣기까지. 틀린 문제는 다시 출제'],
      // 사용자끼리 파는 기능 없음 / 이름은 '상점'
      ['단어장을 사고파는 서점도 있고, 실제 결제가 이뤄집니다.', '단어장을 사는 상점도 있고, 앱 안 결제가 실제로 이뤄집니다.'],
      ['서점 — 단어장을 사고파는 마켓. 앱스토어·구글플레이 결제 연동',
       '상점 — 보석으로 단어장·농장 도구 구매. 앱스토어·구글플레이 결제 연동'],
      ['서점 — 단어장을 골라 담는다', '상점 — 보석으로 단어장과 농장 도구를 산다'],
      ['다른 앱 단어장 6종 가져오기', '퀴즐렛·Anki·엑셀 등 6가지 형식 가져오기'],
      ['게임 요소 — 단어가 자라는 농장, 출석·미션·업적',
       '게임 요소 — 단어가 씨앗에서 당근으로 자라는 농장, 연속 학습·미션·업적'],
      // 예문 수치 정정
      ['5만여 단어 사전에 예문 2만 6천여 개를 생성하고, 사람이 아니라 검증 스크립트가 먼저 확인하게 만들어 오류 없이 마감했다',
       '사전 예문 4만 6천여 개의 단어 강조 위치를 사람이 아니라 형태소 분석과 검증 스크립트가 먼저 확인하게 만들었다'],
      ['관리자 도구에서 조건만 주면 단어장이 자동 생성된다 — 형식을 강제해 파싱 실패와 중복을 걸러냈다',
       '관리자 도구에서 조건만 주면 AI가 단어장 초안을 만든다 — 정해진 형식으로 받고 중복 단어는 자동으로 뺀다'],
      ['예문 강조는 3단계로 — 무료 형태소 분석으로 대부분 처리하고, 실패한 것만, 그래도 남은 것만 AI를 부른다',
       '예문 강조는 3단계 — 무료 형태소 분석 → 규칙 검색 → 그래도 안 되는 것만 AI'],
      // 푸시는 2026-03
      ['2025 하반기 — 앱 구축. 소셜 로그인·푸시·결제·카메라 인식을 앱과 연결하고 웹 화면을 전면 개편',
       '2025 하반기 — 앱 구축. 소셜 로그인·결제·카메라 인식을 앱과 연결하고 웹 화면을 전면 개편, 2026.01 iOS 출시'],
      // 망각 곡선은 고정 그림이고 지금 앱에서 열 수 없다
      ['학습한 단어가 씨앗에서 당근으로 자라는 화면으로 지금 상태를 한눈에 보여줍니다.',
       '단어마다 다음 복습일과 복습 효과가 따로 계산되고, 학습한 단어는 씨앗에서 당근으로 자라는 화면으로 상태를 보여줍니다.'],
      ['내가 학습한 단어들이 자라는 농장', '단어마다 자라는 단계와 다음 복습일이 따로 계산된다'],
      ['홈 — 오늘 할 학습과 업적', '홈 — 내 단어 농장과 오늘 할 복습'],
    ],
    dropMedia: ['fsrs-lifecycle.mp4', 'mypage-dark.png'],
  },

  // ───────────────────────────────────────────────────── OpenDay
  openday: {
    fields: { year: '2023 — 현재 · 운영 중' },
    replace: [
      // 투표는 타입 이름만 있고 편집·렌더러 모두 없음. 지도·BGM은 모듈이 아님
      ['모듈 조합 — RSVP·방명록·투표·계좌·갤러리·영상·BGM·지도·일정표를 행사에 맞게 켜고 끕니다',
       '모듈 조합 — 참석 응답·방명록·계좌·갤러리·영상·일정표·지도를 행사에 맞게 켜고 끄고, 배경음악도 고를 수 있습니다'],
      // 카테고리 정정 (돌잔치는 베이비 하위, 부고·추모 누락)
      ['웨딩뿐 아니라 돌잔치·비즈니스·문화 행사까지 10개 카테고리를 지원합니다.',
       '웨딩·돌잔치부터 세미나·전시·부고까지 10개 행사 카테고리를 지원합니다.'],
      ['웨딩만이 아님 — 돌잔치·생일·교육·비즈니스·모임·스포츠·문화·기념까지 10개 행사 카테고리',
       '웨딩만이 아님 — 웨딩·돌잔치부터 생일·교육·비즈니스·모임·스포츠·전시·부고까지 10개 행사 카테고리'],
      ['{"value":"10개","label":"웨딩·돌잔치·비즈니스·문화까지 행사 카테고리"}',
       '{"value":"10개","label":"웨딩·돌잔치부터 세미나·전시·부고까지 행사 카테고리"}'],
      ['{"value":"20+","label":"RSVP·방명록·지도·BGM 등 조합 가능한 모듈"}',
       '{"value":"20여 종","label":"인사말·갤러리·참석 응답·방명록 등 켜고 끄는 섹션"}'],
      ['필요한 기능만 조합 — RSVP·방명록·갤러리·지도·BGM·일정표 등 20종 이상을 모듈로 켜고 끄기',
       '필요한 기능만 조합 — 인사말·갤러리·참석 응답·방명록·일정표 등 20여 종을 켜고 끄기'],
      ['필드/모듈을 분리한 JSON 스냅샷과 렌더러 계약으로 20종 이상을 한 모델에 담음',
       '필드/모듈을 분리한 JSON 스냅샷과 렌더러 계약으로 20여 종을 한 모델에 담음'],
      // 제작 시간 측정 근거 없음
      ['몇 분이면 완성 — 템플릿 선택 → 정보 입력 → 발행까지 한 흐름, 디자인 경험 불필요',
       '한 흐름으로 완성 — 템플릿 선택 → 정보 입력 → 발행까지, 디자인 경험 불필요'],
      ['디자인 경험 없이도 템플릿을 골라 몇 분 만에 모바일 초대장을 만들고',
       '디자인 경험 없이도 템플릿을 골라 모바일 초대장을 만들고'],
      // 방명록 조회는 공개 상태 — RSVP에만 해당
      ['발행 이후가 비는 문제 — 공개 쓰기/소유자 읽기 권한 분리, 중복 방지 방문 로그, contain 합성 공유 썸네일',
       '발행 이후가 비는 문제 — 참석 응답은 누구나 남기되 조회는 주최자만, 중복 방지 방문 로그, contain 합성 공유 썸네일'],
      // 카피 토큰은 2단계
      ['웨딩 문구가 다른 행사에 새는 문제 — 모듈 config → 콘텐츠 → 카테고리 프리셋 순의 카피 토큰과 의미 단위 표면 토큰',
       '웨딩 문구가 다른 행사에 새는 문제 — 사용자가 쓴 제목 → 행사별 기본 문구 순으로 정해, 같은 화면이 행사에 따라 다른 말을 하게 했다'],
      // 운영 상태 명시
      ['이후 재구축(V3) — 현재 저장소의 커밋 작성자는 본인 단독으로 확인됩니다.',
       '2026 재구축(V3) — 혼자 전면 재구축해 지금도 운영 중입니다.'],
      // 짝꿍 편집 설명 평이하게
      ['짝꿍 편집 — 초대 링크로 공동 편집자를 연결하고 소유권과 편집 권한을 구분합니다',
       '짝꿍 편집 — 링크 하나로 배우자를 초대해 함께 고치되, 삭제·주소 변경·응답 조회는 만든 사람만 할 수 있게 나눴습니다'],
      // 캡션 정정
      ['하객이 받는 실제 초대장 — 인사말·일정·장소·갤러리·참여 모듈',
       '하객이 받는 초대장 — 인사말·일정·장소·오시는 길'],
      ['참석 정보 — 참석/불참·측·인원·식사·메시지',
       '참석 응답 관리 — 참석 여부·인원·식사·연락처를 한 표로, CSV로 내려받기 (데모 데이터)'],
      ['카테고리별 템플릿 카탈로그', '10개 행사 카테고리, 12종 템플릿 카탈로그'],
    ],
    // 조작이 없는 시연 영상 / 커버와 중복 / 에디터 프리뷰 첫 화면과 중복
    dropMedia: ['openday-editor-compose.mp4', 'openday-published-invitation.png'],
  },
}

let touched = 0
for (const [slug, plan] of Object.entries(PLAN)) {
  const row = db.prepare('select id, summary, metrics, sections from projects where slug=?').get(slug)
  if (!row) { console.log(`[skip] ${slug} 없음`); continue }

  let summary = row.summary || ''
  let metrics = row.metrics || ''
  let sections = row.sections || ''
  let hits = 0, miss = []

  // 일부 본문은 일반 공백 대신 줄바꿈 방지 공백(U+00A0)을 쓴다. 둘을 같은 것으로 보고 찾는다.
  const asPattern = (t) =>
    new RegExp(t.replace(/[.*+?^${}()|[\]\\]/g, '\\$&').replace(/[ \u00a0]/g, '[ \\u00a0]'), 'g')

  for (const [oldS, newS] of plan.replace || []) {
    const re = asPattern(oldS)
    let found = false
    for (const key of ['summary', 'metrics', 'sections']) {
      const cur = { summary, metrics, sections }[key]
      if (!cur) continue
      re.lastIndex = 0
      if (!re.test(cur)) continue
      re.lastIndex = 0
      const next = cur.replace(re, () => newS)
      if (key === 'summary') summary = next
      else if (key === 'metrics') metrics = next
      else sections = next
      found = true
    }
    if (found) hits++; else miss.push(oldS.slice(0, 42))
  }

  // 미디어 제거 (파일명 기준)
  let dropped = 0
  if (plan.dropMedia && plan.dropMedia.length) {
    const idByName = new Map(db.prepare('select id, filename from media').all().map((m) => [m.filename, m.id]))
    const dropIds = new Set(plan.dropMedia.map((f) => idByName.get(f)).filter(Boolean))
    const parsed = JSON.parse(sections)
    for (const sec of parsed) {
      if (!sec.media) continue
      const before = sec.media.length
      sec.media = sec.media.filter((m) => !dropIds.has(m.mediaId))
      dropped += before - sec.media.length
    }
    sections = JSON.stringify(parsed)
  }

  const sets = ['summary=?', 'metrics=?', 'sections=?']
  const vals = [summary, metrics, sections]
  for (const [k, v] of Object.entries(plan.fields || {})) { sets.push(`${k}=?`); vals.push(v) }
  db.prepare(`update projects set ${sets.join(', ')}, updated_at=? where id=?`).run(...vals, now, row.id)

  console.log(`[${slug}] 치환 ${hits}/${(plan.replace || []).length} · 미디어 제거 ${dropped}` +
    (miss.length ? `\n   └ 못 찾음: ${miss.join(' / ')}` : ''))
  touched++
}

console.log(`\n[done] 프로젝트 ${touched}건 갱신`)
db.close()
